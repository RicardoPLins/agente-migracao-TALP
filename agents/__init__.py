"""Shared wrappers for the repository's agents.

These wrappers provide import-friendly functions over scripts that may have
non-importable filenames (e.g., with hyphens) and help compose agents together.
"""
