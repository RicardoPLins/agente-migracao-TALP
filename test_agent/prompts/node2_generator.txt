You are a test generator. Produce one Python test file `test_equivalence.py` implementing tests from the provided `test_plan`.

Requirements:
- Use `pytest` and lightweight mocking (`responses` or `unittest.mock`).
- Implement tests for each `test_scenarios` entry in the plan.
- Each test must assert equivalence: `assert result_original == result_migrated` (or explain differences in a comment).
- Output ONLY valid Python code (no surrounding explanation).

Input placeholders:
```python
{original_code}
```

```python
{migrated_code}
```

```json
{test_plan}
```

TEST PLAN (JSON):
```json
{test_plan}
```