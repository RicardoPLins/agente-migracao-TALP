You are an analyzer. Receive two Python modules: ORIGINAL (urllib) and MIGRATED (requests).

Produce a concise JSON test plan with:
- `original_functions`: list of public functions, params, endpoints (method+path), returns, error handling
- `migrated_functions`: same structure for migrated code
- `equivalence_pairs`: pairs of functions to test and any behavioral diffs
- `test_scenarios`: array of test cases (id, category, function, description, mock_setup, expected_original, expected_migrated, priority)
- `coverage_notes`: short notes about hard-to-test areas

Return ONLY valid JSON. Use placeholders `{original_code}` and `{migrated_code}` as input.

Input:
```python
{original_code}
```

```python
{migrated_code}
```